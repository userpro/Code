#include <cstdio>
#include <cstring>
#include <algorithm>
#include <vector>

using namespace std;
#define MAXN 100010
#define LL unsigned long long
int n,k;
int num[MAXN*2];

int main()
{
    while (scanf("%d%d",&n,&k)!=EOF)
    {
        for (int i=0;i<n;i++)
            scanf("%d",&num[i]);
        
    }
    return 0;
}